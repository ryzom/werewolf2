1) if mesh
	2) if not water
		3) if not lod
		4) build base mesh
		5) build mesh
		5) make lightmap
		6) if mrm
			7) build CMRMParameters
				* NLods
				* Divisor
				* SkinReduction
				* DistanceFinest
				* DistanceMiddle
				* DistanceCoarsest
			8) get blendShape list
			9) if MRM Skinned
				10) build CMeshMRMSkinned
			12) if just MRM
				13) build CMeshMRM
			14) optimize material usage
		15) if mesh
			16) build standard CMesh
			

// link the common configuration file
RootConfigFilename = "common.cfg";

NSHost = "localhost";

WindowStyle = "WIN";

AcceptInvalidCookie = 1;

NegFiltersDebug = { "NET" };

DontUseAES = 1; // added temp. by sfb
DontUseStdIn = 1;

// pacs data for the landscape.
RetrieverBankName = "data/pacs/snowballs.rbank";
GlobalRetrieverName = "data/pacs/snowballs.gr";

UpdateTimeout = 2;

DatabaseHost = "localhost";		// name of the host where the mysql database is (empty string mean localhost)
DatabaseName = "wwshard";		// name of the database where we can find needed info
DatabaseUser = "root";			// if we need a login to access the database
DatabasePass = "sp4nky";		// if we need a password to access the database

//ListenAddress = "10.17.18.63:51001";
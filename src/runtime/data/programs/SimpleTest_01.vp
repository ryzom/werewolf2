!!VP1.0
# c[0..3] == MODELVIEW_PROJECTION

DP4 o[HPOS].x, c[0], v[0];
DP4 o[HPOS].y, c[1], v[0];
DP4 o[HPOS].z, c[2], v[0];
DP4 o[HPOS].w, c[3], v[0];

# c[4] == some color set by the user
MOV o[COL0], c[4];
MOV o[TEX0], v[8];

END
